func main():
    var sum = 0.0
    var flip = -1.0
    for (i in 1 to 10000001):
        flip = -flip
        //sum += flip / (i*2 - 1)
        sum += flip / (i<<1 - 1)
        //sum += flip / i
    end
    return sum * 4.0
    //return
end
